フローチャートの検証結果をご報告します。概ね正しいですが、いくつかの重要な訂正と補足が必要です。

1. **harvest.py の役割分担（正確です）**:
   - `queue`: `UserPromptSubmit` と `PostToolUse` でトリガー。イベントを非同期（高速）で JSONL キューに積みます。
   - `worker`: `Stop`（各ターンの終わり）でトリガー。キューを消化し、L1 (Ideas/) / L2 (Meta/Promotions/) の判定・作成を行います。
   - `flush`: `SessionEnd` でトリガー。残りのキューを消化し、最終昇格と L3 (References/ ドラフト) 作成を行います。

2. **SessionEnd の処理順序（修正が必要）**:
   - 実際は `session-memory.sh stop` → `harvest.py flush` → `session-distill.sh` の順で実行されます。
   - つまり `harvest.py` が先で、その後に `distill.py + claude -p` のバッチ処理が走ります。

3. **`/promote` の行き先（不足があります）**:
   - コード（`promote.py`）上、`PROMOTE_ALLOWLIST = ("References/", "Ideas/")` と定義されています。
   - したがって、行き先は `References/` だけでなく **`Ideas/` も含まれます**。

4. **省略されている重要な要素（抜け漏れ）**:
   - **`session-memory.sh` の存在が完全に抜けています。** これは `SessionStart` から `SessionEnd` まで常時稼働し、`Meta/AI Sessions/` に生ログを書き込む中核コンポーネントです。
   - `SessionEnd` で動く `distill.py` は、この `session-memory.sh` が記録した生ログを入力ソースとしています。これをフローチャートに明記しないと、データの出どころが不明になります。

【提案するフロー修正案】
```
Claude Code で作業
      │
      ├─ session-memory.sh (常時) ─→ Meta/AI Sessions/ (生ログ)
      │
      ├─ harvest.py (常時) ───────→ Ideas/ or Meta/Promotions/
      │
      └─ SessionEnd
           ├─ 1. harvest.py flush
           └─ 2. session-distill.sh
                  ├─ distill.py + claude -p (生ログを入力) ─→ Meta/Promotions/ (ドラフト)
                  └─ session-summarizer ───────────────────→ Daily note ### 要約 (AI)

Meta/Promotions/ → [人間がレビュー] → /promote → References/ or Ideas/
```
